<p id="notice"><%= notice %></p>

<h1>Listing Foos</h1>

<table>
  <thead>
    <tr>
      <th>Int number</th>
      <th>Float number</th>
      <th>Decimal number</th>
      <th>Date</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @foos.each do |foo| %>
      <tr>
        <td><%= foo.int_number %></td>
        <td><%= foo.float_number %></td>
        <td><%= foo.decimal_number %></td>
        <td><%= foo.date %></td>
        <td><%= foo.name %></td>
        <td><%= link_to 'Show', foo %></td>
        <td><%= link_to 'Edit', edit_foo_path(foo) %></td>
        <td><%= link_to 'Destroy', foo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Foo', new_foo_path %>
